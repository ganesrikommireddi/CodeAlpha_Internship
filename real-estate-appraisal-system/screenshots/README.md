# Screenshots

Add screenshots here after running the project, for example:

- `eda_price_distribution.png` — price distribution / boxplot from the notebook
- `correlation_matrix.png` — correlation heatmap
- `model_comparison.png` — MAE / RMSE / R² bar chart comparing the three models
- `streamlit_app.png` — the Streamlit prediction interface in action

Reference them in the main `README.md` once added, e.g.:

```markdown
![Streamlit App](screenshots/streamlit_app.png)
```
